

# Replace StateTag with State Outline Images

**File:** `src/components/Blog/BlogPostContent.tsx`

**Change:** Replace lines 12–22 (`ROUTE66_STATES` constant and `StateTag` component) with the new `ROUTE66_STATE_IMAGES` map and image-based `StateTag` component exactly as provided. No other lines changed.

